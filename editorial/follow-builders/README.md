# Follow Builders 上游资产

这目录用于承接原本位于 Obsidian `Daisy_OS/.cursor/skills/follow-builders/` 中、对日报生成质量直接生效的上游资产。

当前先迁入两类内容：

- `prompts/`：控制摘要、翻译、总装格式的提示词模板
- `default-sources.json`：默认追踪的 podcast、blog、X builder 清单

这些文件目前还没有接入本仓库的新生成引擎，但已经成为后续独立化改造的本地事实源，不再只能依赖 Obsidian skill 目录。
